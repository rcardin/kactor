package `in`.rcard.kactor

import kotlin.time.Duration

class TimerScheduler {
    fun <K, T> startSingleTimer(timerKey: K, msg: T, delay: Duration) {
        TODO()
    }

    fun <K> cancel(timerKey: K) {
        TODO()
    }
}
